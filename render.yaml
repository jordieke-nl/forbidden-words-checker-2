services:
  - type: web
    name: forbidden-words-checker-2
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
    plan: free 